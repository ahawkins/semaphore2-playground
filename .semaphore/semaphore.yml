version: "v1.0"
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Warm Caches"
    task:
      jobs:
        - name: "Install 2.4"
          commands:
            - rvm install 2.4
        - name: "Install 2.5"
          commands:
            - rvm install 2.5
