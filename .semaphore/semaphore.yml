version: "v1.0"
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "World"
    task:
      jobs:
        - name: Test
          commands:
            - sudo pip install pipenv
            - checkout
            - pipenv install -d
            - env | sort
            - pipenv run test
